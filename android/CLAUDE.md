# android/

Generated by `expo prebuild`. Do NOT manually edit unless necessary.

## Critical: Regeneration Gotchas

`expo prebuild --clean` wipes this entire folder. After regeneration, re-apply:

1. **Kotlin version pin** — `android/build.gradle`: `classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:${kotlinVersion}")` pinned to 1.9.25
2. **local.properties** — must contain `sdk.dir=/Users/ammar.robb/Library/Android/sdk`

## NFC Config

NFC permissions and intent filters are managed via `app.json` plugins (`react-native-nfc-manager`), not manually in AndroidManifest.xml.

## Build

```bash
npx expo run:android  # builds debug APK
```

Debug APK output: `android/app/build/outputs/apk/debug/app-debug.apk`
